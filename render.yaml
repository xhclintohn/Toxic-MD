services:
  - type: web
    name: Toxic-MD
    env: docker
    region: Frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm run toxictech"
    envVars:
      - key: PORT
        value: "3000"